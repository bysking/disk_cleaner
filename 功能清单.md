# 磁盘清理工具 — 功能清单

> 专业级磁盘空间分析与清理工具 | 作者: bysking | 联系: bysking@qq.com

---

## 一、核心扫描能力

| 序号 | 功能 | 说明 |
|------|------|------|
| 1.1 | 递归目录扫描 | 使用 `os.scandir()` 高效遍历任意目录树 |
| 1.2 | 多线程扫描 | 可配置线程数（Spinbox 控件动态调整），默认 4 线程，上限 `CPU数 × 2` |
| 1.3 | 扫描中断 | 支持用户随时停止正在进行的扫描任务 |
| 1.4 | 实时进度反馈 | 每个线程当前扫描路径 + 进度条 + 状态文字，三重实时反馈 |
| 1.5 | 节流 UI 更新 | 以 ~10Hz 频率批量更新界面，避免扫描高峰期界面卡顿 |
| 1.6 | 自底向上累计 | 扫描完成后自叶子节点向上累加目录大小，数据准确可靠 |
| 1.7 | 系统目录自动跳过 | 内置跳过 `$Recycle.Bin`、`System Volume Information`、`.Spotlight-V100` 等系统目录 |
| 1.8 | 自定义排除列表 | 用户可手动输入排除目录，支持逗号、中文逗号（，）、顿号（、）、换行、空格多种分隔符 |
| 1.9 | 权限异常容错 | 扫描时遇到无权限访问的文件/目录自动跳过，不中断整体流程 |

---

## 二、目录导航

| 序号 | 功能 | 说明 |
|------|------|------|
| 2.1 | 面包屑导航 | 可点击的路径面包屑，一键跳转到任意上级目录 |
| 2.2 | 双击进入子目录 | 双击列表中的目录即可深入查看 |
| 2.3 | 返回上级 | 一键回到上一级目录 |
| 2.4 | 在系统文件管理器中打开 | 点击路径列，在 Windows/macOS 原生文件管理器中定位该文件/文件夹 |

---

## 三、选择与清理

| 序号 | 功能 | 说明 |
|------|------|------|
| 3.1 | 复选框选择 | 使用 Unicode 图标（☐☑▶○），直观区分文件/目录选中状态 |
| 3.2 | 递归选择 | 选中目录自动递归选中其下所有子项 |
| 3.3 | 全选/取消全选 | 一键全选或取消所有已选项 |
| 3.4 | 动态计数按钮 | "全选""取消全选"按钮实时显示已选项目数量 |
| 3.5 | 删除前确认弹窗 | 列出前 10 项及总大小，防止误删 |
| 3.6 | 安全删除顺序 | 按路径长度降序排列，先删深层再删浅层，避免父目录先删导致子项无法删除 |
| 3.7 | 后台线程删除 | 删除操作在后台线程执行，不阻塞界面 |
| 3.8 | 删除结果反馈 | 完成后弹窗展示成功/失败信息 |

---

## 四、安全防护

| 序号 | 功能 | 说明 |
|------|------|------|
| 4.1 | 关键目录保护 | 尝试选择 `Windows`、`Program Files`、`System`、`Library`、`Applications` 等系统目录时弹出红色警告 |
| 4.2 | 强制选择确认 | 用户须明确勾选"我理解上述风险"才能强制选择受保护目录 |
| 4.3 | 权限容错 | 扫描/删除时遇到无权限文件自动跳过，不会导致程序崩溃 |

---

## 五、界面交互

| 序号 | 功能 | 说明 |
|------|------|------|
| 5.1 | 现代 GUI | Tkinter + ttk，1100×700 默认窗口，最小 800×500，自动居中 |
| 5.2 | 五列表格 | 名称（含图标）| 扩展名 | 大小（格式化）| 大小（原始字节）| 路径 |
| 5.3 | 大小降序排列 | 目录和文件自动按大小从大到小排序，快速定位空间大户 |
| 5.4 | 路径截断显示 | 超长路径自动截断，保留首尾可见，中间用省略号代替 |
| 5.5 | 每目录 2000 项上限 | 防止超大目录导致界面卡死，超出时显示省略指示 |
| 5.6 | 菜单栏 | "帮助 → 授权管理"、"关于" |
| 5.7 | 隐形版权水印 | 零宽空格（ZWSP）水印，不可见但可复制 |
| 5.8 | 倒计时显示 | 试用/限时授权时，右下角实时显示剩余时间 |
| 5.9 | 作者信息栏 | 底部常驻显示作者与联系方式 |
| 5.10 | DPI 自适应 | Windows 下自动适配高分屏，字体/控件不模糊 |

---

## 六、授权与商业化

| 序号 | 功能 | 说明 |
|------|------|------|
| 6.1 | 机器指纹绑定 | MAC 地址 + CPU ID → SHA256，一机一码 |
| 6.2 | 24 小时免费试用 | 每台机器仅可试用一次，无法通过卸载重装重置 |
| 6.3 | 双通道试用记录 | Windows：隐藏文件 + 注册表；macOS/Linux：隐藏文件 |
| 6.4 | 隐藏文件属性 | Windows 下试用文件自动设为隐藏属性 |
| 6.5 | HMAC 密钥签名 | Base32 编码 + HMAC-SHA256 签名，密钥不可伪造 |
| 6.6 | 多种授权类型 | 永久授权 / 按天限时 / 按秒限时 |
| 6.7 | 激活信息持久化 | 按平台保存到标准位置（%APPDATA% / ~/Library / ~/.diskcleaner） |
| 6.8 | 运行时强制校验 | 启动时检查授权，过期后弹窗阻断并提示重新激活 |
| 6.9 | 实时倒计时阻断 | 试用或限时授权到期后，实时显示倒计时并阻断使用 |
| 6.10 | 重新激活入口 | 菜单栏 "帮助 → 授权管理" 随时可更换授权密钥 |
| 6.11 | 离线密钥生成器 | 厂商专用 CLI 工具，支持批量生成密钥、清除本地状态、查看机器指纹 |

---

## 七、跨平台支持

| 平台 | 支持程度 | 特色能力 |
|------|----------|----------|
| Windows | 完整支持 | 默认扫描 C:\，注册表试用记录，os.startfile 打开路径，DPI 自适应 |
| macOS | 完整支持 | 默认扫描 /，subprocess open 定位文件，隐藏文件记录试用 |
| Linux | 部分支持 | 试用/授权持久化正常，无原生文件管理器集成 |

---

## 八、构建与部署

| 序号 | 功能 | 说明 |
|------|------|------|
| 8.1 | 零运行时依赖 | 全部使用 Python 标准库，无需 pip install 第三方包 |
| 8.2 | Windows 一键构建 | `build_win.bat` 自动创建 venv → 安装 PyInstaller → 打包单文件 exe（约 11 MB） |
| 8.3 | macOS 一键构建 | `build_mac.sh` 自动打包 .app + DMG 镜像 |
| 8.4 | PyInstaller 打包 | 单文件可执行文件，无需安装 Python 运行环境 |
| 8.5 | 已构建成品 | `dist/DiskCleaner.exe` 可直接分发给用户 |

---

## 九、技术架构

```
disk_cleaner.py          主程序（~1227 行）
  ├── Scanner 类          多线程目录扫描器（ThreadPoolExecutor + 工作队列 + 毒丸模式）
  ├── DiskCleanerApp 类   Tkinter GUI 应用（授权检查、扫描、导航、选择、清理）
  └── Node 数据类         树节点模型（path/name/size/is_dir/children）

license_module.py        授权模块（~317 行）
  ├── 机器指纹采集         MAC + CPU ID → SHA256
  ├── 密钥编解码           Base32 + HMAC-SHA256
  ├── 试用持久化           隐藏文件 + Windows 注册表
  └── 激活管理             按平台 JSON 持久化

activation_dialog.py     激活对话框（~137 行）
keygen_license.py        厂商密钥生成器 CLI（~177 行）
```

---

## 十、功能亮点速览

- **快**：多线程扫描，CPU 密集型 I/O 充分并行
- **准**：自底向上累加，目录大小不遗漏
- **安全**：关键目录保护 + 删除确认 + 后台线程执行
- **好用**：面包屑导航、双击深入、排序、全选、路径截断
- **商业化完整**：试用 → 授权 → 到期阻断 → 重新激活，全链路闭环
- **跨平台**：Windows / macOS / Linux 均可运行
- **零依赖**：标准库构建，打包单文件即可分发
