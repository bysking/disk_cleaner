# DiskCleaner — 磁盘空间分析与清理工具

> **一分钟找到磁盘空间的"罪魁祸首"，一键清理，立竿见影。**

---

## 你的磁盘是不是也这样？

- C 盘又红了，但不知道是什么占用了空间
- 项目里一堆 `node_modules`、`__pycache__`、`.git`、`venv`、`dist`，每个都好几个 GB
- 下载文件夹堆积如山，却不知道哪个文件最大
- 装了各种清理软件，结果它们自己占了几百 MB

**DiskCleaner 就是为了解决这些问题而生的。**

---

## 什么是 DiskCleaner？

DiskCleaner 是一款 **专业的磁盘空间分析与清理工具**，帮你：

1. **可视化扫描** — 递归分析任意目录，以清晰的树形列表展示每个文件和文件夹的大小
2. **精准定位** — 自动按大小排序，空间大户一目了然
3. **一键清理** — 勾选不需要的内容，确认后即刻释放磁盘空间
4. **安全保护** — 系统关键目录自动保护，误选会弹出红色警告

> 零第三方依赖，打包成单文件可执行程序，双击即用。

---

## 核心优势

### 快 — 多线程扫描，不等

采用多线程并行扫描技术，充分利用 CPU 核心。扫描 C 盘这样的大型目录，速度是单线程工具的多倍。扫描过程中可实时看到每个线程正在分析的路径，进度一目了然。

### 准 — 自底向上累加，不遗漏

目录大小不是简单估算，而是扫描完成后从最深层的子文件逐层向上累加，确保每一个字节都被计算在内。

### 稳 — 关键目录保护，防误删

尝试选择 `Windows`、`Program Files`、`System`、`Library`、`Applications` 等系统目录时，程序会弹出醒目的红色警告对话框，列出所有风险项。只有明确勾选"我理解上述风险"后才能继续。

### 易用 — 点击即达，不用学

| 操作 | 方式 |
|------|------|
| 进入子目录 | 双击 |
| 返回上级 | 点击"返回"按钮 |
| 跳转任意层级 | 点击面包屑导航 |
| 在文件管理器中打开 | 点击路径列 |
| 全选/取消 | 一键操作，按钮显示数量 |

---

## 界面预览

```
+------------------------------------------------------------------+
| 路径: [C:\________________________] [浏览] 线程: [4] [扫描] [停止] |
| [ ] 排除目录: ▾                                                     |
| [多行文本框 - 输入要排除的目录名]                                    |
+------------------------------------------------------------------+
|  > C: > Users > Administrator > Desktop                           |
+------------------------------------------------------------------+
| ☑ 名称              │ 扩展名 │ 大小     │ 路径                    |
| ▶ node_modules      │        │ 2.3 GB   │ C:\...\node_modules     |
| ▶ .git              │        │ 856 MB   │ C:\...\.git             |
| ▶ dist              │        │ 412 MB   │ C:\...\dist             |
| ○ large_video.mp4   │ .mp4   │ 1.8 GB   │ C:\...\large_video.mp4  |
| ...                                                                  |
+------------------------------------------------------------------+
| 已选择: 3 项  [全选(3)]  [取消全选(3)]  [清理(3)]                  |
|                                                                    |
|              剩余时间: 23:59:59                    bysking 联系:  |
+------------------------------------------------------------------+
```

---

## 适用场景

| 场景 | 痛点 | DiskCleaner 怎么做 |
|------|------|-------------------|
| 开发者 | `node_modules` 越来越大，不知道哪些项目占空间最多 | 扫描项目目录，按大小排序，一键选中清理 |
| 运维人员 | 服务器磁盘告警，需要快速定位大文件 | 扫描 `/var` 或任意目录，秒级定位空间大户 |
| 普通用户 | C 盘红了但不知道是什么占的 | 扫描 C 盘，看看哪些文件夹最大 |
| 数据分析师 | 下载目录堆积，历史数据集占用大量空间 | 按大小排序，快速识别不需要的旧文件 |
| 设计/视频工作者 | 缓存文件、临时文件占用大量空间 | 扫描工作目录，精准定位并清理 |

---

## 技术实力

- **零运行时依赖** — 全部使用 Python 标准库，无需安装任何第三方包
- **多线程架构** — ThreadPoolExecutor + 工作队列 + 毒丸模式，扫描稳定高效
- **节流 UI 更新** — 扫描高峰期也不会卡界面
- **DPI 自适应** — Windows 高分屏下字体和控件清晰不模糊
- **安全删除顺序** — 先删深层再删浅层，不会出现"父目录已删除，子项无法删除"的尴尬
- **后台线程执行** — 清理时界面不卡死，结果弹窗反馈

---

## 授权方案

| 方案 | 说明 |
|------|------|
| **免费试用** | 每台机器 24 小时完整功能试用，无需注册 |
| **限时授权** | 按天/按秒灵活配置，适合短期项目使用 |
| **永久授权** | 一次购买，永久使用 |

> 试用期间享有全部功能，无任何限制。试用到期后弹窗提示激活。
>
> 购买授权请联系：**bysking@qq.com**

---

## 跨平台

| 系统 | 状态 |
|------|------|
| Windows 10/11 | 完整支持 |
| macOS | 完整支持 |
| Linux | 核心功能可用 |

---

## 常见问题

**Q：试用过期了怎么办？**
A：联系 bysking@qq.com 获取授权密钥，在菜单栏 "帮助 → 授权管理" 中输入即可。

**Q：会不会误删系统文件？**
A：程序内置了系统关键目录保护，尝试选择 Windows、Program Files、System 等目录时会弹出红色警告，需要手动确认才能继续。

**Q：扫描会不会很慢？**
A：多线程扫描充分利用 CPU，通常几十 GB 的目录几分钟就能完成。扫描过程中可随时中断。

**Q：需要安装 Python 吗？**
A：不需要。已打包成独立可执行文件，双击即可运行。

---

## 获取方式

- **Windows**：直接运行 `DiskCleaner.exe`（约 11 MB，独立可执行文件）
- **macOS**：运行 `.dmg` 镜像中的 `DiskCleaner.app`

> 获取最新版本或购买授权，请联系：**bysking@qq.com**

---

*DiskCleaner — 让磁盘空间管理变得简单。*
